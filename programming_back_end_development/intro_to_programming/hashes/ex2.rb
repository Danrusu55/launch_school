
hash_one = {name:'Alex',age:42}
hash_two = {hair:'Brown',city:'Sydney'}

puts "Hash One: #{hash_one}"
puts "Hash Two: #{hash_two}"

puts "Merge Hashes #{hash_one.merge(hash_two)}"

puts "Hash One: #{hash_one}"
puts "Hash Two: #{hash_two}"

puts "Merge! Hashes #{hash_one.merge!(hash_two)}"

puts "Hash One: #{hash_one}"
puts "Hash Two: #{hash_two}"

